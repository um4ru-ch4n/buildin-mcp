version: '3.8'

services:
  buildin-mcp:
    build: .
    environment:
      - TRANSPORT_MODE=http
      - HTTP_PORT=3000
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=production
    ports:
      - "${HTTP_PORT:-3000}:3000"
    restart: unless-stopped
